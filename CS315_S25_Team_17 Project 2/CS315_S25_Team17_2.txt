begin
/* 
This is program 2
*/

float values[] = new float[5];
int sum = 0;

// Populate the array
for (int i = 0; i < 5; i = i + 1) {
    values[i] = i * 2.5;
    sum = sum + values[i];
}

display("Total sum is: ");
display(sum);
end
